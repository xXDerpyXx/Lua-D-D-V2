
function getStat(player,stat) -- input = player table, stat in mind
	return --stat value
end

function setStat(player,stat, val) -- input = player table, stat in mind, new value
	return --new player table
end

function savePlayer(player) -- save the player to the player file (doesn't exist yet)
	return --if player file exist to be saved
end 
